# Release tooling

Scripts for signing and notarizing the desktop app and for publishing GitHub Releases.
Populated in Phase 6. Until then the upstream scripts in `scripts/` remain the source of truth.
