# Strict by default: every package declares what it imports.
auto-install-peers=true
strict-peer-dependencies=false
# Keep native builds deterministic and off the network where possible.
engine-strict=true
